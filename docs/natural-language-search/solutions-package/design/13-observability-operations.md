# Observability

Log one line per phrase submission: whether the model parse succeeded, whether the fallback parser succeeded, and which bounds were present (price, theme, age) as yes or no. Do not log the phrase.

Log the MySQL error the handler already returns as a failed page. Do not add a new error page.

Trace: existing search error behavior, security design.
